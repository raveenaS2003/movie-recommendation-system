# Movie Recommendation System — Requirements
# Install with:  pip install -r requirements.txt

# Core data & ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# NLP / Embeddings (for semantic recommendations)
sentence-transformers>=2.6    # downloads ~90MB MiniLM model on first run

# Optional: faster vector search (comment out if install fails)
# faiss-cpu>=1.7              # uncomment for large datasets (>10k movies)

# Web UI
streamlit>=1.35

# Utilities
tqdm>=4.66
